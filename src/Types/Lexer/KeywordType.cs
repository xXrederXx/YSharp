namespace YSharp.Types.Lexer;

public enum KeywordType
{
    VAR,
    AND,
    OR,
    NOT,
    IF,
    THEN,
    ELIF,
    ELSE,
    FOR,
    TO,
    STEP,
    WHILE,
    FUN,
    END,
    RETURN,
    CONTINUE,
    BREAK,
    TRY,
    CATCH,
    IMPORT
}